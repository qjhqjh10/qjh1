
# 文件作用速查 v15.3.1

（v15.3.1: AIChatWindow/index.tsx 拖拽修复（手柄 flex 跟随底缘+自动增高 220 上限）+ RoleTemplate.worldKbFileIds/scenarioKbFileIds（settings.ts，两组互斥）+ AISettingsTab SettingFilePicker 组件（世界观/场景卡片正下方各内嵌勾选区，互斥联动）+ BridgeContextBuilder 两组并入检索（独立于KB开关，与 selectedKbFileIds 合并；提示词分段点名文件名 + read_file 读对应组文件引导，构建时查 kbService.list）+ BridgeContextBuilder.test.ts +4 例；v15.3.0: AIChatWindow/index.tsx 一体化输入框（仿 DeepSeek）+ #工具选择 popover（ALL_TOOLS 32 工具）+ buildToolHintText（utils.ts，软提示注入）+ resumeInject.test.ts +4 例；v15.2.1 新增: src/utils/modelPricing.ts（内置价格表+联网查价解析）+ modelPricing.test.ts 13 例、ai:fetch-model-pricing IPC 链（aiHandlers/preload/electron.d.ts/fileService）、ModelSettingsTab 自动调价+联网查价按钮；v15.2.0 新增: src/utils/configMigration.ts（默认配置字段级迁移，v15.2.1 补错位定价修正）+ configMigration.test.ts 7 例；v15.1.0 新增: scripts/package-dist.sh + scripts/zip-dist.mjs（一键安全打包）、chapterSplit.test.ts、RewriteProject.summaryConfig；v15.0.0 新增: scripts/test-ai-agent.mjs、electron/main.ts userData 改造；删除: scripts/test-ai-conversation.mjs、app:check-update IPC 链）

## Electron 主进程 (electron/)

| 文件 | 职责 |
|------|------|
| `main.ts` | 应用入口，创建窗口，注册 IPC handler（v14.6.1: 单实例锁；v14.7.0: setAppUserModelId；**v15.0.0: 绿色便携 userData 改造——dev: 源码/userdata/、打包: exe 旁/userdata/，任何 getPath 前 setPath；app:check-update 移除——版本更新改腾讯在线文档**） |
| `ipc/netFetch.ts` | **v14.7.0 新增** — 统一网络栈（net.fetch = Chromium 系统代理+系统证书；非 Electron 环境回退全局 fetch）；主进程 9 处网络调用全部替换 |
| `ipc/pathResolution.ts` | 路径解析（v14.6.1: 黑名单 re-export 自 src/utils/pathBlacklist.ts，任意盘符；**v15.0.0: containsBackupSegment 判定保留但不再硬拒绝——备份目录保护移至 fileToolHandlers 写工具软约束**） |
| `preload.ts` | contextBridge 安全 API |
| `ipc/aiHandlers.ts` | OpenAI 协议 (chat/stream/tools/image)，API key 明文存储 |
| `ipc/anthropicHandlers.ts` | Anthropic 协议 (fetch+SSE)，cache_control 缓存 |
| `ipc/fileHandlers.ts` | 文件读写 (listDir/read/write/delete/rename) |
| `ipc/fileToolHandlers.ts` | AI 文件工具执行（find_files dir_path safeResolve containment 加固 v14.4.0；**v15.0.0: WRITE_TOOLS 备份目录软约束（写工具拒绝+引导"读备份→写原件"、读放行）+ MAX_BACKUPS_PER_FILE 10→3**） |
| `ipc/statsHandlers.ts` | Token/会话统计（parseAuditJsonl 纯函数聚合 + deleteSession 完整 id v14.4.0） |
| `ipc/kbHandlers/helpers.ts` | KB 辅助（getEmbedding 返回 usage + buildEmbeddingUsageEntry 记账 v14.4.0） |
| `ipc/__tests__/statsHandlers.test.ts` | **v14.4.0 新增** — parseAuditJsonl 聚合单测 |
| `ipc/__tests__/kbEmbedding.test.ts` | **v14.4.0 新增** — buildEmbeddingUsageEntry 单测 |
| `ipc/*Handlers.ts` | 项目/KB/导出/统计/风格/续写/仿写/改写/HTTP/浏览器/MCP（shellHandlers/lspHandlers/agentHandlers 已删） |

## Agent 运行时 (src/agent/)

| 文件 | 职责 |
|------|------|
| `chatBridgeFactory.ts` | **v14.0.0 新增** — BaseChatBridge 基类（双协议 Bridge 主体，抽象 createAdapter/abortStream/getRunId/getEnableThinkingPlan） |
| `V4AnthropicChatBridge.ts` | Anthropic 协议 Bridge（薄包装，仅协议差异） |
| `V4AgentChatBridge.ts` | OpenAI 协议 Bridge（薄包装，仅协议差异） |
| `ChatBridgeInterface.ts` | Bridge 接口 + 共享类型 + createChatBridge 工厂 |
| `V4SystemPrompt.ts` | 核心提示词唯一来源 (~3,600t) + 写作手册索引（v14.0.2: MD 文件化提示词已删除） |
| `V4SecurityFence.ts` | 四层安全围栏 |
| `bridge/toolExecutorFactory.ts` | 共享工具执行管线 |
| `runtime/V4UnifiedRuntime.ts` | 统一主循环 + 渐进披露 + 自愈恢复 + 读限制器 + 完成检测 + 任务清单/验收提示/taskProgress (v14.2.0) + 验收督促闸门/truncated 中断标记/截断续写修复 (v14.3.0) + 清单门控修复/refreshToolSet 安全收窄/resumeTaskProgress 续跑恢复 (v14.5.0) |
| `runtime/ToolExecutor.ts` | 单工具执行 (matchedTools存储；v14.2.0: PARALLEL_READ_TOOLS/SERIAL_WRITE_TOOLS 子代理并行拆分；v14.3.0: +verify_task 分片并行 + subagentSummaries 快照收集；v14.5.0: +subagent_ask 分片 + 子代理 per-call abort + skipOpHistory) |
| `runtime/AgentEventEmitter.ts` | 事件总线 |
| `context/BridgeContextBuilder.ts` | 上下文组装 (规则+角色+搜索+项目信息) |
| `context/ContextCompressor.ts` | 3 阶段压缩 (70/80/90%) |
| `context/CacheInvalidator.ts` | 文件变更缓存失效 |
| `context/ContextAssembler.ts` | 已退化 (仅保留 domainsForPath) |
| `context/ContractExecutor.ts` | 工具结果契约过滤 (含tool_search；v14.5.0: HTTP/浏览器 4 工具保留 detail 4000 截断) |
| `context/FileCache.ts` | 文件读缓存 |
| `skills/ToolRegistry.ts` | 工具注册/执行/权限（needsApproval(name, args?) 条件审批 v14.4.0；v14.5.0: 有 approvalGate 时缺省参数也走 gate） |
| `skills/tools/index.ts` | 32 工具聚合 + 核心工具集（v14.8: +kb_analyze） |
| `skills/tools/toolSearchTools.ts` | tool_search (按需发现+matchedTools结构化返回) |
| `skills/tools/fileTools.ts` | 9 文件工具（find_files approvalGate：scope=computer 才审批 v14.4.0；v14.5.0: list_directory(broad:true) DANGEROUS_ASK+approvalGate） |
| `skills/tools/kbTools.ts` | 3 知识库工具 |
| `skills/tools/harnessTools.ts` | 1 Harness 工具 (list_rules) |
| `skills/tools/*.ts` | 其他工具 (note/image/template/project/prompt/http/browser) |
| `skills/tools/subagentTools.ts` | **v14.1.1 新增** — analyze_file/edit_file_task/verify_task/subagent_ask 四个子agent 委托工具（v14.2.0: +verify_task；v14.3.0: +subagent_ask 会话追问 + detail 上限放宽 + verify summary 三态；v14.5.0: verify JSON 配对提取 + 关键词降级） |
| `subagent/SubagentService.ts` | **v14.1.1 新增** — 子agent 工厂 (isolatedStore+窗口跟随配置+独立审计；v14.2.0: +verify 角色；v14.3.0: 会话池+追问复用+truncated 判定+10 轮+失败轨迹回传；v14.4.0: +find_files + 会话池 4→8 + auditTrail 接线；v14.5.0: 会话池角色校验 + TTL 10min) |
| `subagent/SubagentPrompt.ts` | **v14.1.1 新增** — analyze/edit/verify 三套提示词 (v14.2.0: +验收 JSON 报告；v14.3.0: 输出上限放宽 2500/1200 + 原文摘录) |
| `subagent/createSubagentAdapter.ts` | **v14.1.1 新增** — 双协议路由 (isAnthropicProtocol 同 chatAI) |
| `runtime/adapters/AnthropicAdapter.ts` | Anthropic 格式转换 (合并连续tool消息+cache_control) |
| `runtime/adapters/OpenAIAdapter.ts` | OpenAI 格式转换 (cached_tokens透传) |
| `audit/AuditTrail.ts` | JSONL 审计日志（persist 快照 await 前捕获 v14.4.0；recordApiCall +cost/model） |
| `store/AgentStore.ts` | Zustand Agent 状态 (熔断器/peakPromptTokens) |
| `diagnostics/DiagnosticLogger.ts` | 诊断日志 |
| `utils/taskDetection.ts` | 消息分类 (isPureGreeting/hasTaskKeywords/isKnowledgeOnly；SKILL 复杂度评分体系已删) |
| `utils/taskExtraction.ts` | **v14.1.0 新增** — 编号任务清单提取 (四重门控，宁漏勿错) |
| `utils/tokenEstimation.ts` | Token 估算 (CJK 1.2 / Latin 4.5 实测校准 v14.4.0) |

## 模板文件 (.aiharness/templates/)

| 路径 | 职责 |
|------|------|
| `writing-handbook/outline.md` | 大纲创作指南 |
| `writing-handbook/characters.md` | 角色管理指南 |
| `writing-handbook/chapters.md` | 章节/细纲/润色指南 |
| `writing-handbook/text-processing.md` | 文本处理指南 (分析/导入/提取) |
| `writing-handbook/style-scene.md` | 风格模板/场景模板指南 |
| `writing-handbook/kb-notes.md` | 知识库/草稿笔记指南 |
| `writing-handbook/multi-task.md` | 多任务编排/自由文件指南 |
| *(其他 16 文件)* | 角色/章节/细纲/大纲8Tab/摘要/笔记/KB/场景/风格模板格式参考 |

## UI 组件 (src/components/)

| 文件 | 职责 |
|------|------|
| `ai/AIChatWindow/index.tsx` | AI 聊天窗口 (32场景/Token显示/任务清单进度卡片/子代理简报卡片/快照持久化 v14.3.0；v14.5.0: IME 防护/幻觉检测接线/token 条守卫/resumeTaskProgress 透传；**v15.0.0: UI 重构——胶囊开关/圆角输入卡片/圆形发送按钮/诊断面板开关（showDiagnostics 默认隐藏+移至用量条下）**） |
| `ai/AIChatWindow/components/VirtualMessageList.tsx` | **v14.5.0** — 自动滚动逻辑移入（原 index.tsx scrollRef 死引用） |
| `ai/AIChatWindow/hooks/useConversationState.ts` | **v14.5.0 已删除** — 全仓无引用死代码 |
| `ai/ContextUsageBar.tsx` | Token 用量条 (estimatedContextTokens驱动) |
| `common/useDraggableResizable.ts` | **v14.0.0 新增** — 拖拽/缩放共享 hook（双锚定+8手柄+持久化） |
| `pages/settings/tabs/KnowledgeBaseSettingsTab.tsx` | **v14.0.0 新增** — 知识库设置（agent/generation 分场景 topK/注入上限） |
| `pages/settings/tabs/TokenStatsTab.tsx` | v14.2.0: 来源筛选 + bySource；v14.3.0: 按日折线图/按模型视图/预算全局口径/缓存列；v14.4.0: 会话花费/工具失败/权限拒绝徽章 + embedding 来源 |
| `pages/settings/tabs/agent/OperationHistorySection.tsx` | **v14.3.0 新增** — 操作记录内容组件（设置→Agent→操作记录子标签；OperationHistoryPage 薄容器复用） |
| *(其他不变)* | ... |

## 服务层 (src/services/)

| 文件 | 职责 |
|------|------|
| `fileService.ts` | 统一 IPC 调用层 (chatWithTools 加 source 参数 v14.2.0；getUsage opts +source v14.4.0；v14.5.0: aborted 标记透传) |
| `anthropicService.ts` | Anthropic 协议 (chatAnthropicStream 加 source 参数 v14.2.0；v14.5.0: thinkingBlocks 透传) |
| `chatStorageService.ts` | IndexedDB 对话持久化 (v14.5.0: saveConversations 800ms 防抖合并 + mergeConversations 加载合并 + idbOk 返回) |
| `debugLogService.ts` | 诊断事件日志（v14.4.0: 删 11 个无调用者辅助函数，仅剩 debugEvent/debugApiError） |
| `electronBridge.ts` | IPC 服务聚合（v14.4.0: agentService 已删） |
| `scripts/measure-token-density.mjs` | **v14.4.0 新增** — DeepSeek 分词密度测量工具（tokenEstimation 系数校准） |

## 关键工具 (src/utils/)

| 文件 | 职责 |
|------|------|
| `chatAI.ts` | 双协议路由 |
| `styleInjector.ts` | 风格模板注入 |
| *(其他不变)* | ... |

## v14.6.0 新增/变更文件

| 文件 | 职责 |
|------|------|
| `electron/ipc/pathResolution.ts` | **v14.6.0 新增** — 全自由模式路径解析纯函数（isBlockedSystemPath 系统目录黑名单 / resolveArg 归一化 / safeResolveArg realpath 复核；无 electron 依赖可单测） |
| `electron/ipc/__tests__/pathResolution.test.ts` | **v14.6.0 新增** — 路径解析 13 例单测（中段 ../、混合分隔符、绝对路径、UNC、percent-encoding、系统目录） |
| `src/components/ai/AIChatWindow/__tests__/buildHistoryMessages.test.ts` | **v14.6.0 新增** — buildHistoryMessages 6 例单测（工具轮检测/压缩摘要注入/早期折叠/过滤） |
| `src/components/ai/AIChatWindow/utils.ts` | **v14.6.0** — buildHistoryMessages 从 index.tsx 移入（纯函数可单测）；工具轮检测改用 toolCallSteps（修复死代码）；compressedSummary 注入为 [对话压缩摘要] |
| `src/components/ai/AIChatWindow/index.tsx` | **v14.6.0** — 本地 buildHistoryMessages 已删除（改用 utils 导入） |
| `electron/ipc/fileToolHandlers.ts` | **v14.6.0** — 路径解析接入 pathResolution（全自由）；search_content 全局目录修复；batch_replace 全局替换+备份+校验；find_files dir_path 统一 safeResolve |
| `src/agent/V4SecurityFence.ts` | **v14.6.0** — Layer 3（外部路径审批）移除；Layer 1/2 保留 |
| `src/agent/skills/tools/fileTools.ts` | **v14.6.0** — list_directory(broad)/find_files(computer)/delete_file/rename_file 全部 AUTO |
| `src/agent/runtime/V4UnifiedRuntime.ts` | **v14.6.0** — 每次调用独立 AbortController（超时取消底层流）；无清单完成判定排除 CONTINUATION_RE |
| `src/agent/runtime/adapters/AnthropicAdapter.ts` | **v14.6.0** — signal→abortStream 接线；cache_control 断点移到倒数第二个 system 块；**v14.6.1** — 连续同角色消息合并（截断轮/空响应兜底 400 修复）+ per-request abort |

## v14.7.0 新增/变更文件

| 文件 | 职责 |
|------|------|
| `electron/ipc/netFetch.ts` | **v14.7.0 新增** — 统一网络栈（net.fetch = Chromium 系统代理+系统证书；非 Electron 回退全局 fetch）；主进程 9 处网络调用全部替换 |
| `src/utils/pathBlacklist.ts` | **v14.6.1 新增** — 系统目录黑名单纯函数（零依赖，V4SecurityFence 与主进程 pathResolution 共享单一来源，任意盘符） |
| `electron/ipc/exportHandlers.ts` | **v14.6.1** — 导出路径限制解除（assertSafeExportPath 仅挡系统目录+写保护目录）；对话框默认路径显式指向项目目录 |
| `src/agent/context/CacheInvalidator.ts` | **v14.6.1** — 相对+绝对双 key 失效 + onFileChanged 传绝对路径（GUI 刷新修复）；kb_append_file 目录失效 |
| `src/agent/runtime/RuntimeTypes.ts` | **v14.6.1** — V4AgentRunInput +toolsEnabled；V4AgentRunResult +reasoningContent |
| `src/components/ai/AIChatWindow/utils.ts` | **v14.6.1** — maybeInjectResume/maybeInjectSubagentSummaries 注入 role 改 user；detectHallucination 否定语境排除 |
| `src/components/pages/settings/VersionTab.tsx` | **v15.0.0** — 版本更新改为弹窗→腾讯在线文档（git 更新不可用，app:check-update IPC 已移除；UPDATE_DOC_URL 常量 + Modal 确定/取消/叉叉 + 软件说明） |
| `electron/ipc/aiHandlers.ts` | **v14.6.1** — per-request abort（requestId 精确中止）+ DANGEROUS 死门闩移除 + 全部 OpenAI 实例走 netFetch |
| `electron/ipc/anthropicHandlers.ts` | **v14.6.1** — per-request abort + netFetch |
| `src/agent/runtime/adapters/OpenAIAdapter.ts` | **v14.6.0** — signal→abortStream 接线 |

## v14.8.0 新增/变更文件

| 文件 | 职责 |
|------|------|
| `electron/ipc/responsesConverter.ts` | **v14.8.0 新增** — Responses API 消息转换纯函数（messages→items、tools 转换+web_search 注入、孤儿 tool 裁剪、collectFunctionCalls）；实测约束：call_id 语义/thinking 禁 function tool_choice/全量 items 回传 |
| `src/agent/runtime/adapters/ResponsesAdapter.ts` | **v14.8.0 新增** — Responses API 协议适配器（implements ProtocolAdapter，source 区分 main/subagent，requestId 精确中止） |
| `src/agent/runtime/adapters/responsesRouter.ts` | **v14.8.0 新增** — shouldUseResponses 单一路由决策源（openai+nativeWebSearch+deepseek v4） |
| `scripts/export-tool-schemas.ts` | **v14.8.0 新增** — 工具 schema 导出脚本（原 .mjs 删除；从 ALL_TOOLS 导入生成，--check 名称+内容深比较） |
| `src/agent/skills/tools/subagentTools.ts` | **v14.8.0** — 惰性化重构（顶层 import SubagentService→@/store 链在 Node 挂起根因解除；runSubagent 改 executor 内 await import；SUBAGENT_TOOL_NAMES 本地镜像） |
| `src/agent/skills/tools/kbTools.ts` | **v14.8.0** — +kb_analyze（第 32 工具，Agentic RAG 委托子代理深度分析）；kb_search 排除集改经 ctx.kbInjectedFileIds |
| `electron/ipc/aiHandlers.ts` | **v14.8.0** — +ai:responses-chat handler（流式聚合+降级 chat.completions）；buildThinkingParams 共享（ai:chat/chat-stream 接入，pipeline thinking 对称） |
| `src/agent/context/BridgeContextBuilder.ts` | **v14.8.0** — 模块单例 injectedKbFileIds 删除 → 实例字段 per-run；+excludeKbFileIds 跨 run 排除；返回 injectedFileIds；原生联网跳过 DDG；searchResultCount 默认 5 |
| `src/agent/runtime/RuntimeTypes.ts` / `V4UnifiedRuntime.ts` / `ToolExecutor.ts` | **v14.8.0** — kbInjectedFileIds 全链路（ContextAssemblerFn 返回 → 实例字段 → execCtx → ToolExecutionContext）；run 结果持久化 |
| `src/agent/skills/types.ts` + `src/agent/state/types.ts` | **v14.8.0** — ToolExecutionContext 双副本 +kbInjectedFileIds（同步维护） |
| `src/components/ai/AIChatWindow/index.tsx` | **v14.8.0** — 消息 kbInjectedFileIds 持久化 + sendMessage excludeKbFileIds 透传；联网搜索按钮显示「(原生)」 |
| `src/types/fileOps/toolDefs.ts` | **v14.8.0** — +ResponsesChatResult（toolCalls 归一化 {id,name,arguments}） |
| `src/agent/__tests__/ResponsesAdapter.test.ts` / `BridgeContextBuilder.test.ts` + `electron/ipc/__tests__/responsesConverter.test.ts` | **v14.8.0 新增** — 21 例新单测 |
| `scripts/check-consistency.sh` | **v14.8.0** — C1 改调用 `--check` 判定（名称+内容） |
| `electron/ipc/httpHandlers.ts` | **v14.6.0** — redirect:'manual' 逐跳 SSRF 复查（最多 3 跳） |
| `scripts/tool-schemas.json` | **v14.6.0** — 从真实注册表重新生成（36→31；原静态快照含已删除工具） |

## v15.0.0 新增/变更文件

| 文件 | 职责 |
|------|------|
| `scripts/test-ai-agent.mjs` | **v15.0.0 新增** — Agent 复杂任务测试脚本（9 场景 CA1-CA9：细纲流水线/子代理全链路/搜索批量删除/跨run续跑/知识库RAG/重命名JSON修复/角色扮演/渐进披露联网KB维护/项目管理备份恢复大文件；专用测试项目 agent-proj+backup-test 不碰用户项目；mock 与主进程行为对齐含 delete_project 路径护栏；SSE 按行解析） |
| `scripts/test-ai-conversation.mjs` | **v15.0.0 已删除** — 旧 32 简单场景测试脚本（被 test-ai-agent.mjs 替代） |
| `electron/main.ts` | **v15.0.0** — 绿色便携 userData（dev: 源码/userdata/，打包: exe 旁/userdata/，任何 getPath 前 setPath）；app:check-update 移除 |
| `electron/preload.ts` + `src/types/electron.d.ts` | **v15.0.0** — checkUpdate 移除 |
| `src/components/pages/settings/VersionTab.tsx` | **v15.0.0** — 版本更新弹窗→腾讯在线文档（UPDATE_DOC_URL 常量 + Modal） |
| `src/components/ai/AIChatWindow/index.tsx` + `components/AgentStateBar.tsx` | **v15.0.0** — UI 重构（胶囊开关/圆角输入卡片/圆形发送按钮/圆点拖拽手柄/诊断面板开关 showDiagnostics/就绪提示移除/分隔线加深） |
| `electron/ipc/pathResolution.ts` + `fileToolHandlers.ts` | **v15.0.0** — 备份软约束（containsBackupSegment 判定 + WRITE_TOOLS 写工具引导性拒绝 + MAX_BACKUPS_PER_FILE 3） |
| `src/agent/skills/tools/fileTools.ts` + `src/agent/V4SystemPrompt.ts` | **v15.0.0** — delete_file 描述修正 + 备份目录语义认知 |
| `.gitignore` | **v15.0.0** — +userdata/ imitation_projects/ rewrite_templates/ scripts/test-*.mjs |

## v15.1.0 新增/变更文件

| 文件 | 职责 |
|------|------|
| `scripts/package-dist.sh` | **v15.1.0 新增** — 一键安全打包脚本（tsc+打包安全测试 → electron-vite build → electron-builder --dir → 删除 release/win-unpacked/userdata → node.dll 完整性验证 → zip → unzip 复检无 userdata）；用法 `bash scripts/package-dist.sh [版本号]` |
| `scripts/zip-dist.mjs` | **v15.1.0 新增** — 分发 zip 打包（archiver v8 ESM ZipArchive）；userdata 存在即拒绝的硬闸门（防泄漏/防对方机器僵尸配置） |
| `src/utils/__tests__/chapterSplit.test.ts` | **v15.1.0 新增** — 章节拆分误判回归测试 5 项（第X节课/回合/集结尾/卷土重来排除 + 句尾保护 + 正常标题不受影响） |
| `src/types/rewrite.ts` | **v15.1.0** — +RewriteSummaryConfig 接口 + DEFAULT_SUMMARY_CONFIG 默认值 + RewriteProject.summaryConfig |
| `src/components/pages/RewriteWorkspacePage.tsx` | **v15.1.0** — 总结阶段（parseAnalysisResponse 容错/callAIWithTimeout 120s/重总结保留旧数据/批量全失败不推进）；改写阶段（改写本章按钮/插入信息勾选区 rewriteContextOptions/清除本章数据按钮+确认弹窗）；改写字数语义修正（buildRewritePrompt 加料文案/buildSegmentRewritePrompt 段占比加料）；loadAllRewrites isPassing 补 target；effectiveConfigId 已删配置回退全局；顶栏模板只读+查看按钮（RewritePromptModal 定位）；模板已删显示 |
| `src/utils/rewriteSegmentUtils.ts` | **v15.1.0** — buildSegmentRewritePrompt +extraWordTarget/chapterWordCount（场景段按占比换算加料目标） |
| `src/utils/textUtils.ts` | **v15.1.0** — 章节拆分误判排除（CHAPTER_FALSE_POSITIVE_HEAD/TAIL 排除表 + CHAPTER_LINE_ENDING_PROTECT 句尾保护）；6 处调用方全局生效 |
| `src/components/pages/RewritePage.tsx` | **v15.1.0** — 项目设置弹窗 520→780px 可编辑（模型/并发线程/改写字数/模板/总结信息 settingsDraft + 保存） |
| `src/components/pages/RewriteCreateWizard.tsx` | **v15.1.0** — 7 步向导 +「总结信息」步骤（preview 后）；创建时保存 summaryConfig |
| `src/components/pages/RewritePromptModal.tsx` | **v15.1.0** — +复制按钮（DocumentDuplicateIcon，名称自动加（N））+删除改 ConfirmModal +initialTemplateId 定位 prop |
| `src/components/ai/AIChatWindow/index.tsx` | **v15.1.0** — +深度思考开关（BoltIcon，ToggleButton disabled 态）+原生联网可开关（点击切换 nativeWebSearch）+删除对话/审批弹窗约束浮窗内（transform 包含块） |
| `electron/ipc/aiHandlers.ts` | **v15.1.0** — ai:listModels 超时 8s→15s + 错误提示可操作化（默认 openai 地址引导选服务商） |

## v15.2.1 新增/变更文件

| 文件 | 职责 |
|------|------|
| `src/utils/modelPricing.ts` | **v15.2.1 新增** — 模型价格模块：MODEL_PRICE_PRESETS 内置参考价表（DeepSeek/OpenAI/Anthropic/Gemini/智谱/千问/Kimi 30+ 模型，2026-08 核实）+ lookupModelPrice（精确→最长前缀）+ parseOpenRouterModels/matchLiveModel（联网查价解析与匹配） |
| `src/utils/__tests__/modelPricing.test.ts` | **v15.2.1 新增** — 价格表 13 例（DeepSeek 映射/大小写/前缀优先/未收录/OpenRouter 解析/匹配） |
| `src/utils/configMigration.ts` | **v15.2.1** — 修正 v15.1 错位定价（0.02/1/2 → 1/2/0.02，仅三字段全等于错位值才整体修正）+ NEW 目标值改正 |
| `src/types/settings.ts` | **v15.2.1** — DEFAULT_MODEL_CONFIG 定价修正：inputPricePerM 1 / outputPricePerM 2 / cacheHitPricePerM 0.02（v15.1 错位为 0.02/1/2）+ 定价字段注释明确口径 |
| `electron/ipc/aiHandlers.ts` | **v15.2.1** — +ai:fetch-model-pricing handler（netFetch 拉取 OpenRouter /api/v1/models，15s 超时，免密钥，返回 {models, source, fetchedAt}） |
| `src/components/pages/settings/tabs/ModelSettingsTab.tsx` | **v15.2.1** — 选模型自动调价（handleModelChange → applyPreset 填 货币+输入+输出+缓存命中+上下文）+「🔗 联网查价」按钮 + 状态提示（priceActions 插槽） |
| `electron/preload.ts` + `src/types/electron.d.ts` + `src/services/fileService.ts` | **v15.2.1** — fetchModelPricing IPC 链（AIAPI.fetchModelPricing → preload → aiService） |
| `.gitignore` | **v15.2.1** — +continuation_project_dirs/（运行时目录补齐） |

## v15.2.0 新增/变更文件

| 文件 | 职责 |
|------|------|
| `src/utils/configMigration.ts` | **v15.2.0 新增** — 默认模型配置字段级迁移（migrateDefaultConfigs：仅当字段仍等于 v15.0.0 旧默认值才更新为 v15.1/15.2 新默认；mainCurrency 已设置时币种不动；App.tsx loadConfigs 后执行 + 变更时 saveConfigs；v15.2.1 补错位定价修正段） |
| `src/utils/__tests__/configMigration.test.ts` | **v15.2.0 新增** — 迁移 4 例（整体迁移/自定义字段保留/mainCurrency 保护/新模板无变化；v15.2.1 改 7 例：+错位三元组修正/部分自定义保护） |
| `src/types/settings.ts` | **v15.2.0** — DEFAULT_MODEL_CONFIG DeepSeek 化（provider/apiUrl/model/protocol anthropic/nativeWebSearch true/currency CNY/mainCurrency；定价 v15.2.1 修正为 1-2-0.02） |
| `src/components/pages/RewriteWorkspacePage.tsx` | **v15.2.0** — 精准改写约束（未标记段落逐字保留+情色保护/needsRewrite=false 保护批量过滤+单章提示/enforceTemplateRewrite markers 匹配扩展）；场景合并规则恢复允许合并（相似场景合并防重复改写） |
| `src/utils/rewriteSegmentUtils.ts` | **v15.2.0** — 段改写输出范围约束（只输出该段）；mergeAdjacentSegments 死代码删除（零引用） |
| `src/services/chatStorageService.ts` | **v15.2.0** — 过时注释修正（v14.6 后 fileHandlers 无 globalAppDataPath 白名单） |
| `src/App.tsx` | **v15.2.0** — loadConfigs 后接入 migrateDefaultConfigs |
