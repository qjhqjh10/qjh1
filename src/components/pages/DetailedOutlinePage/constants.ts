import type React from 'react'

export const os: React.CSSProperties = { padding: '10px 12px', borderRadius: 8, border: '1px solid rgba(0,0,0,0.08)', background: '#fff', marginBottom: 8 }
export const ost: React.CSSProperties = { fontSize: 11, fontWeight: 700, color: '#2d2520', marginBottom: 6, paddingBottom: 6, borderBottom: '1px solid rgba(0,0,0,0.06)' }
export const osb: React.CSSProperties = { fontSize: 11, lineHeight: 1.6, color: '#4a3f38', whiteSpace: 'pre-wrap' }

export const btnSecondary: React.CSSProperties = {
  padding: '8px 18px', borderRadius: 8,
  border: '1px solid rgba(0,0,0,0.08)', background: '#fff',
  color: '#6b5e54', fontSize: 12, fontWeight: 600,
  cursor: 'pointer', fontFamily: 'inherit',
}

export const btnPrimary: React.CSSProperties = {
  padding: '8px 18px', borderRadius: 8,
  border: 'none', background: '#7c3aed', color: '#fff',
  fontSize: 12, fontWeight: 600, cursor: 'pointer', fontFamily: 'inherit',
}
